export type UserLoginDTO = {
    email: string;
    password: string;
};

export type UserCreateDTO = {
    email: string;
    password: string;
};

export type UserDTO = {
    id: number;
    email: string;
};